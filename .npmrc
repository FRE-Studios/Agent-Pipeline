# .npmrc - npm configuration for agent-pipeline

# Enforce Node.js version requirement (requires Node 18+)
# Prevents installation on incompatible Node versions
engine-strict=true

# Ensure package-lock.json is used for reproducible installs
package-lock=true

# Save dependencies to package.json when installing
save=true
